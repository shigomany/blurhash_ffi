name: dart_blurhash_ffi
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.0

# resolution: workspace

# Add regular dependencies here.
dependencies:
  ffi: ^2.1.4
  hooks: ^1.0.0
  code_assets: ^1.0.0
  native_toolchain_rs: ^1.0.0
  logging: ^1.3.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
  ffigen: ^20.1.0